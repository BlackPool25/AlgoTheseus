# AlgoTheseus frontend

React 19 + Vite + TypeScript UI: Monaco editor, React Flow CFG chart,
state panel, 12 container visuals, trace scrubber.

## Commands (run from `frontend/`)

```bash
bun install --frozen-lockfile
bun run dev        # → http://localhost:5173
bun run build      # production build to dist/
npx playwright test
```

Root docs: [CONTRIBUTING](../CONTRIBUTING.md) · async `/jobs` → [docs/API.md](../docs/API.md)
· [Full API → ../docs/API.md](../docs/API.md) · [Self-host → ../docs/SELF-HOSTING.md](../docs/SELF-HOSTING.md)
